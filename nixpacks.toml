[phases.setup]
providers = ["python"]

[phases.install]
cmds = ["pip install -r requirements.txt"]

[phases.build]
cmds = ["mkdir -p static/uploads instance"]

[start]
cmd = "gunicorn app:app --bind 0.0.0.0:$PORT"
